import { SecuritySettingsPage } from "@tgoliveira/secure-auth/react";
import { APP_SLUG } from "@/lib/brand";

export default function Page() {
  return <SecuritySettingsPage appSlug={APP_SLUG} />;
}
