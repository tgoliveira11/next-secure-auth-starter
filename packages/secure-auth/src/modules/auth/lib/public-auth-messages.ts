/** Public-safe registration failure message — does not reveal account existence. */
export const GENERIC_REGISTRATION_ERROR =
  "Unable to complete registration with the provided information.";

/** Public-safe passkey login options failure message — does not reveal account/passkey state. */
export const GENERIC_PASSKEY_LOGIN_OPTIONS_ERROR =
  "Unable to start passkey sign-in with the provided information.";
